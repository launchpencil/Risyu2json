{
  "name": "risyu2json",
  "version": "1.0.0",
  "private": true,
  "description": "Extract CampusSquare timetable data into JSON",
  "main": "dist/extract.js",
  "scripts": {
    "build": "tsc",
    "extract": "npm run build && node dist/extract.js"
  },
  "dependencies": {
    "bplist-parser": "^0.3.2",
    "jsdom": "^24.1.1"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.1",
    "@types/node": "^22.10.10",
    "typescript": "^5.7.3"
  }
}
